package controller.game;

/**
 * The enum Response error.
 */
public enum ResponseError {
    /**
     * Select pawn error response error.
     */
    SELECT_PAWN_ERROR,
    /**
     * Move pawn error response error.
     */
    MOVE_PAWN_ERROR
}
